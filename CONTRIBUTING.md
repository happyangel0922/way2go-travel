# 貢獻指南

感謝您對 Way2Go Travel 的興趣！我們歡迎所有形式的貢獻。

## 如何貢獻

### 報告 Bug

如果您發現了 bug，請：

1. 檢查 [Issues](https://github.com/yourusername/way2go-travel/issues) 中是否已有相同報告
2. 如果沒有，請創建新的 Issue，包括：
   - 清晰的標題
   - 詳細的描述
   - 重現步驟
   - 預期行為 vs 實際行為
   - 您的環境信息（OS、瀏覽器、Node 版本等）

### 提出功能建議

1. 檢查 [Issues](https://github.com/yourusername/way2go-travel/issues) 中是否已有相同建議
2. 創建新的 Issue，標題以 `[Feature]` 開頭
3. 詳細描述您的想法和使用場景

### 提交代碼

1. **Fork 倉庫**
   ```bash
   git clone https://github.com/yourusername/way2go-travel.git
   cd way2go-travel
   ```

2. **創建功能分支**
   ```bash
   git checkout -b feature/your-feature-name
   ```

3. **安裝依賴**
   ```bash
   pnpm install
   ```

4. **進行更改**
   - 遵循現有的代碼風格
   - 添加必要的註釋
   - 更新相關文檔

5. **測試您的更改**
   ```bash
   pnpm dev
   ```

6. **提交更改**
   ```bash
   git add .
   git commit -m "feat: 描述您的更改"
   ```

7. **推送到您的 Fork**
   ```bash
   git push origin feature/your-feature-name
   ```

8. **創建 Pull Request**
   - 清晰描述您的更改
   - 連結相關的 Issue
   - 確保所有檢查都通過

## 代碼風格指南

### TypeScript/React

- 使用 TypeScript 進行類型安全
- 使用函數式組件和 Hooks
- 遵循 React 最佳實踐
- 使用有意義的變數名稱

### CSS/Tailwind

- 優先使用 Tailwind CSS 工具類
- 避免過度自訂 CSS
- 使用現有的設計 Token
- 保持響應式設計

### 命名約定

- **組件** - PascalCase（例如 `HeroSection.tsx`）
- **文件** - kebab-case（例如 `hero-section.tsx`）
- **變數/函數** - camelCase（例如 `handleScroll`）
- **常數** - UPPER_SNAKE_CASE（例如 `MAX_WIDTH`）

### 提交信息

遵循 Conventional Commits 格式：

```
type(scope): subject

body

footer
```

類型：
- `feat` - 新功能
- `fix` - 修復 bug
- `docs` - 文檔更新
- `style` - 代碼風格更改（不影響功能）
- `refactor` - 代碼重構
- `perf` - 性能優化
- `test` - 添加或修改測試
- `chore` - 構建過程、依賴管理等

示例：
```
feat(hero): add parallax background effect

Implement parallax scrolling effect for hero section background image.
Improves visual depth and user engagement.

Closes #123
```

## 開發工作流

1. **本地開發**
   ```bash
   pnpm dev
   ```

2. **代碼檢查**
   ```bash
   pnpm check
   ```

3. **代碼格式化**
   ```bash
   pnpm format
   ```

4. **構建**
   ```bash
   pnpm build
   ```

## Pull Request 檢查清單

- [ ] 我已經 fork 了倉庫
- [ ] 我創建了功能分支
- [ ] 我的代碼遵循項目風格指南
- [ ] 我已經添加了必要的註釋
- [ ] 我已經更新了相關文檔
- [ ] 我的更改沒有引入新的警告
- [ ] 我已經測試了我的更改
- [ ] 我的提交信息清晰有意義

## 項目結構

```
way2go-travel/
├── client/src/
│   ├── components/    # React 組件
│   ├── hooks/         # 自訂 Hooks
│   ├── pages/         # 頁面組件
│   ├── lib/           # 工具函數
│   └── index.css      # 全局樣式
├── server/            # 後端代碼
├── shared/            # 共享代碼
└── README.md          # 項目文檔
```

## 常見問題

### 如何添加新組件？

1. 在 `client/src/components/` 中創建新文件
2. 編寫組件代碼
3. 在需要的地方導入並使用
4. 更新相關文檔

### 如何修改樣式？

1. 優先使用 Tailwind CSS 工具類
2. 如需自訂樣式，在 `client/src/index.css` 中添加
3. 遵循現有的設計 Token

### 如何添加新的頁面？

1. 在 `client/src/pages/` 中創建新組件
2. 在 `client/src/App.tsx` 中添加路由
3. 在導航中添加鏈接

## 行為準則

我們致力於提供一個友好、包容的社區。請：

- 尊重他人的觀點
- 提供建設性的反饋
- 避免騷擾或歧視性語言
- 報告不當行為

## 許可

通過貢獻，您同意您的貢獻將在 MIT 許可下發布。

## 聯絡方式

有問題？請通過以下方式聯絡我們：

- 在 [Issues](https://github.com/yourusername/way2go-travel/issues) 中提問
- 發送電子郵件至 support@way2go-travel.com

感謝您的貢獻！🙏
